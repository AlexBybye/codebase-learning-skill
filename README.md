# Code Analyzer v2 完整对比报告

## 1. 结论与证据边界

v2 已覆盖当前项目运行时。现有 `.claude-plugin/`、`agents/`、`skills/` 共 8 个运行时文件，无额外旧文件、无符号链接，Claude 严格插件校验通过。

## 2. 总体能力对比

| 维度     | 旧版                                               | v2                                                    | 结果                                 |
| -------- | -------------------------------------------------- | ----------------------------------------------------- | ------------------------------------ |
| 产品定位 | 移动端模块分析器                                   | Web、Mobile、Backend、Data/Infra、AI 与混合仓库学习器 | 顶层域从 1 个增至 5 个               |
| 编排方式 | 固定三阶段串行流水线，详情阶段固定调用四个子 Skill | 浅层发现、五域路由、仅加载 relevant 域合同            | 空域不再启动专项分析                 |
| 单元模型 | 固定 Page/API/Card/Feature 分类                    | 从真实入口建立少量业务流单元                          | 适配非 UI 与跨域仓库                 |
| 证据合同 | 仅部分模板有完整字段级证据                         | 所有关键事实和调用边统一`source/evidence`           | 直接事实、配置、推断、缺失边界可区分 |
| 跨域链路 | 不是一级合同                                       | 混合仓库至少输出一条逐跳有证据的端到端路径            | UI/API/Data/AI 可闭环解释            |
| 恢复方式 | Stage/分类状态，详情偏追加                         | 稳定 unit ID、产物/章节/证据锚点、整体重建            | 降低重复章节与全量重跑风险           |
| 项目耦合 | 含组织专用默认值和移动端假设                       | 专有名称只能从目标源码发现                            | 已知专有标识从 30 降至 0             |

## 3. 静态量化

| 指标                   |   旧版 |     v2 |    变化 |            降幅 |
| ---------------------- | -----: | -----: | ------: | --------------: |
| 运行时文件             |     11 |      8 |      -3 |           27.3% |
| 总行数                 |  2,047 |    453 |  -1,594 |           77.9% |
| 总字节                 | 83,279 | 25,481 | -57,798 |           69.4% |
| Skill 指令行数         |  1,806 |    429 |  -1,377 |           76.2% |
| Claude 入口 Agent 行数 |    154 |     16 |    -138 |           89.6% |
| 最大路由指令行数       |  1,960 |    445 |  -1,515 |           77.3% |
| 最大路由指令字节       | 80,043 | 25,244 | -54,799 |           68.5% |
| 顶层 Skill             |      7 |      1 |      -6 |           85.7% |
| 条件域 references      |      0 |      5 |      +5 |            新增 |
| 固定详情分发           |      4 |      0 |      -4 | 100% 结构性消除 |
| 顶层仓库域             |      1 |      5 |      +4 |        5 倍覆盖 |
| AI 明确阶段            |      0 |     10 |     +10 |            新增 |
| 含私有默认值的文件     |      4 |      0 |      -4 |            清零 |

这些是文件与指令合同的静态测量，不等于真实 Token 或耗时收益。

## 4. 编排架构

### 旧版

```text
入口 Agent
  → 全仓扫描与移动分层
  → 固定 Page/API/Card/Feature 划分
  → 固定 Detail 编排
      → API Detail
      → Card Detail
      → Page Detail
      → Feature Detail
```

三个阶段严格串行。四个详情分支按固定顺序执行，即使某类为空也会进入对应任务。大仓模式只改变分批或优先顺序，并不会从后续流水线裁掉无关域。

### v2

```text
薄入口 Adapter
  → 一次浅层文件/配置发现
  → 深入前写五域 route-plan
  → 只加载 relevant 域 reference
  → 按真实入口建立有界业务流 unit
  → 共享证据台账并连接跨域路径
  → 排序后整体重建学习地图
```

v2 始终只调用一个主 Skill，再按证据加载 `R` 个域参考文件，`0 <= R <= 5`。`skipped` 域不得运行专项合同或生成空模板。

## 5. 条件路由的静态指令负载

| 路由场景             | 指令字节 | 相对旧固定流水线降幅 |
| -------------------- | -------: | -------------------: |
| 旧固定流水线         |   80,043 |                   — |
| v2 五域全部 relevant |   25,244 |                68.5% |
| 仅 Web               |   14,809 |                81.5% |
| 仅 Mobile            |   15,141 |                81.1% |
| 仅 Backend           |   14,903 |                81.4% |
| 仅 Data/Infra        |   15,162 |                81.1% |
| 仅 AI                |   16,465 |                79.4% |

上述投影以宿主遵守“只加载 relevant reference”为前提；没有宿主遥测，不能把它表述为实测上下文或延迟下降。

## 6. 五域解释深度

五个域均通过同一组 6 项核心解释门槛：

1. 真实入口或注册关系；
2. 有界业务流单元；
3. 数据与控制流追踪；
4. 外部或缺失边界；
5. 状态、结果、错误或降级；
6. 字段级 `source/evidence`。

| 域         | 入口与结构                                                 | 调用链与边界                                                                       | 明确输出类别 | 核心门槛 |
| ---------- | ---------------------------------------------------------- | ---------------------------------------------------------------------------------- | -----------: | -------: |
| Web        | 浏览器挂载、路由、页面、关键组件、状态、loader/action      | 用户或生命周期事件到请求、响应映射、渲染/导航、空态/错误                           |            5 |      6/6 |
| Mobile     | App target、页面/组件、导航、状态及可选数据层              | 生命周期/事件到状态、客户端/本地存储、UI 消费                                      |            6 |      6/6 |
| Backend    | HTTP/RPC/GraphQL、事件、worker/job、schedule、queue、CLI   | middleware/鉴权/校验到 service、持久化、集成、response/ack/retry/fallback          |            5 |      6/6 |
| Data/Infra | persistence、cache、messaging、build/deploy、observability | schema/migration/transaction、缓存回源/失效、消息收发/确认、部署声明和遥测         |            5 |      6/6 |
| AI         | 被真实入口使用的 AI 实现与十阶段流水线                     | ingestion、retrieval、prompt/model、tools、结构化输出/引用、评测、安全、成本和降级 |            6 |      6/6 |

“与 Mobile 等价”指五域都能输出入口、业务流、边界和源码证据闭环，不表示每个专项都已穷尽。全域仍缺统一的相关测试解释；异步取消、消息顺序/背压、隔离/锁、streaming/session cancellation 等高级并发语义仍是 P1 深挖项。

### 移动端保留与取舍

v2 保留页面/组件、布局、状态、导航、生命周期、客户端出站调用、本地存储/cache、错误/空态和源码可证视觉字段。

旧版对布局、字体、颜色、图标以及 API 请求/响应字段采用更刚性的逐项模板；v2 改为领域合同，不再强制完全相同的版式。它换取了跨栈覆盖和更小上下文，但如果目标是像素级 UI 资产盘点或固定 API 文档格式，旧版模板的输出刚性更高。

## 7. 证据与防幻觉合同

每条关键路由理由、结论、调用边和缺失环节必须同时写出：

- `source: code`：由当前源码具体符号直接证明；
- `source: config`：由仓库管理的配置、schema、migration、构建或部署键直接证明；
- `source: inferred`：至少两个独立锚点支持，并写明推断过程；
- `source: unavailable`：当前边界无法闭合，并说明检查范围和缺失连接。

证据只能使用相对目标根目录的 `path#symbol`、`path#key` 或行号锚点。绝对用户路径、旧分析文档和仅有依赖名称不能代替实现证据。项目品牌、客户端、协议、模型、设计系统和基础设施名称只能在目标源码或配置真实出现时写入产物。

## 8. 幂等与恢复

| 行为           | 旧版                                     | v2                                                  |
| -------------- | ---------------------------------------- | --------------------------------------------------- |
| 恢复键         | Stage/分类状态                           | 稳定业务流 unit ID                                  |
| completed 定位 | 隐含在分类产物中                         | artifact、section anchors、evidence anchors         |
| 重试写入       | 在详情文件末尾追加                       | 替换单个 unit 区块后排序整体重写                    |
| 旧状态歧义     | 可能再次追加                             | 零/多命中标记`state_inconsistent`，不复制可疑内容 |
| 失败隔离       | 任一 Stage 失败即停止整条流水线          | 不依赖失败单元的其他 unit 可继续                    |
| 确定性         | append、timestamp 和遍历顺序可能影响结果 | 禁止时间/随机 ID；每个稳定 ID 仅一个区块            |

真实二次重试的字节一致性没有运行，本节证明的是指令合同而非运行结果。

## 9. 写入与运行边界

v2 只读取选定目标仓库，只允许在解析并校验后的 `doc/analysis/` 下写分析产物；禁止修改业务代码，默认不运行应用、测试、构建或 migration，不安装依赖、不读取凭据、不联网或调用外部服务，也不清理分析目录中的无关文件。

这些是指令边界，不是操作系统级沙箱。入口仍拥有通用读写工具，因此机械路径隔离尚未证明。

## 10. 验证凭据

| 检查                | 结果                                                                 |
| ------------------- | -------------------------------------------------------------------- |
| 候选逐文件内容清单  | 清理前记录为 8/8 通过                                                |
| 候选与已安装运行时  | 清理前记录为逐字节一致，额外文件 0                                   |
| 当前运行时 symlink  | 0                                                                    |
| Visible 结构套件    | 0 error、0 warning、0 risk finding                                   |
| Sealed 结构套件     | 0 error、0 warning、0 risk finding                                   |
| Claude 严格插件校验 | Claude Code 2.1.150 下通过                                           |
| Skill tree digest   | `b7a5d13e80fe9c257f1732d174a3fbd75b0f4c5d4ebf1a3d3ad7836b88f3c259` |
| 完整运行时内容摘要  | `d2f69cec699aa25930957ba4c0b57e3190eb96d75e3256686c9d97bc930a5692` |

结构检查只能证明文件和合同结构，不能证明宿主激活、自动路由、泛化、运行安全或模型回答质量。